export const metadata = {
  title: "Checkout",
  description: "Pay once and book items across Swadishtt, Grokly and Insta Style in a single checkout.",
  robots: { index: false, follow: true },
};

export default function Layout({ children }) {
  return <>{children}</>;
}
