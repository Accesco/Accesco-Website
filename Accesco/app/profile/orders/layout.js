export const metadata = {
  title: "My Unified Orders | Accesco Living Profile",
  description: "View and manage all your unified active orders, shopping history, and receipts.",
  robots: { index: false, follow: true },
};

export default function Layout({ children }) {
  return <>{children}</>;
}
