export const metadata = {
  title: 'Checkout | InstaStyle',
  description: 'Complete your order with secure checkout. Fast delivery in 15-20 minutes.',
  robots: { index: false, follow: true },
};

export default function CheckoutLayout({ children }) {
  return children;
}
