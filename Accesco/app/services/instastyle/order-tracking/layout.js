export const metadata = {
  title: "Track Order | InstaStyle",
  description: "Track your fast-delivered fashion orders and delivery status from InstaStyle in real-time.",
  robots: { index: false, follow: true },
};

export default function Layout({ children }) {
  return <>{children}</>;
}
