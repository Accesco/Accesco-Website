export const metadata = {
  title: "Precision Health Food Mode | Swadisht",
  description: "Filter and customize dishes based on calories, macros, diet goals and allergen warnings with Healthy Mode.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
