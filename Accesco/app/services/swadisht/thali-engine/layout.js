export const metadata = {
  title: "Custom Thali Engine | Swadisht",
  description: "Build and customize your own perfect meal box, thali and combo dishes with our interactive Thali Engine.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}
