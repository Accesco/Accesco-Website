export const metadata = {
  title: 'Partner as Vendor | Accesco Living',
  description: 'Join Accesco Living as a vendor and grow your business',
};

export default function VendorLayout({ children }) {
  return children;
}
