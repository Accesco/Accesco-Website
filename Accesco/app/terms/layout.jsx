export const metadata = {
  alternates: {
    canonical: 'https://accescoliving.com/terms',
  },
};

import "./style.css";

export default function TermsLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
